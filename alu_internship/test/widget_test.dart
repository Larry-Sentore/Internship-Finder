// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility in the flutter_test package. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

import 'package:flutter_test/flutter_test.dart';

void main() {
  // The default counter smoke test no longer applies now that the app boots
  // through Firebase + auto_route (`App` in lib/main.dart). Pumping it here
  // would require Firebase test doubles that aren't wired up yet, so this is
  // left as a placeholder until that test harness exists.
  testWidgets('placeholder', (WidgetTester tester) async {});
}
